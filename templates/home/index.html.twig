<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservation API - Studios TKOH!</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: white;
            position: relative;
        }

        .status {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            border: 3px solid rgba(255, 255, 255, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            animation: pulse 2s ease-in-out infinite;
            backdrop-filter: blur(10px);
        }

        .status::after {
            content: '✓';
            font-size: 3rem;
            color: white;
            font-weight: bold;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                box-shadow: 0 0 0 0 rgba(255, 255, 255, 0.4);
            }
            50% {
                transform: scale(1.05);
                box-shadow: 0 0 0 15px rgba(255, 255, 255, 0);
            }
        }

        h1 {
            font-size: 3.5rem;
            margin-bottom: 20px;
            font-weight: 600;
            text-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
            letter-spacing: -1px;
        }

        .message {
            font-size: 1.5rem;
            margin-bottom: 80px;
            opacity: 0.95;
            font-weight: 300;
        }

        .footer {
            position: absolute;
            bottom: 40px;
            text-align: center;
        }

        .brand {
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .copyright {
            font-size: 1rem;
            opacity: 0.8;
        }

        /* Responsive */
        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }
            .message {
                font-size: 1.2rem;
                padding: 0 20px;
            }
            .status {
                width: 60px;
                height: 60px;
            }
            .status::after {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <div class="status"></div>
    <h1>Reservation API</h1>
    <p class="message">La API está funcionando correctamente</p>

    <div class="footer">
        <div class="brand">Studios TKOH!</div>
        <p class="copyright">&copy; 2025 Studios TKOH! Todos los derechos reservados.</p>
    </div>
</body>
</html>